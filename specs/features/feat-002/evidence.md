# Evidencia — feat-002: Identidad, roles y perfiles básicos

Estado: evidencia histórica de implementación y validación externa parcial.

> Nota de revisión 2026-07-29: las filas que mencionan invitaciones, Auth0
> Organizations o el callback `/login` corresponden al flujo sustituido. Son
> evidencia histórica, no validan la especificación revisada. Se debe obtener
> evidencia nueva para el selector cliente OTP email y el acceso interno
> preaprovisionado con contraseña y MFA. Desde el 2026-08-01, Google queda
> fuera del MVP y no forma parte de la evidencia de aceptación de esta feature.

| Fecha | Método | Resultado resumido | Criterio | Estado |
|---|---|---|---|---|
| 2026-07-27 | Validación manual no productiva: Auth0 Email Passwordless → Try, modo `code` | Auth0 entregó un correo de prueba con código OTP de seis caracteres. El código mostrado en una captura se considera expuesto y no se conserva. | AC-002-001 / REQ-002-001 | parcial: entrega OTP comprobada; canje desde el flujo real de Universal Login pendiente. |
| 2026-07-27 | Validación manual no productiva: Auth0 Google connection → Try | Auth0 mostró `Successful transaction` para `google-oauth2` en `PIGAR-STAGING-CUSTOMER`. No se conservó ni registró el perfil devuelto. | AC-002-001 / REQ-002-001 | pass: conexión Google de cliente comprobada. |
| 2026-07-27 | Validación manual no productiva: Action Post Login MFA | Se validó enrolamiento y desafío TOTP mediante una condición temporal de prueba; luego se restauró y desplegó la condición exclusiva para `PIGAR-STAGING-ADMIN`. No se conservaron QR, claves ni códigos. | AC-002-002 | pass parcial: factor MFA y restricción de Action verificados; invitación interna pendiente. |
| 2026-07-27 | Configuración de staging informada por el usuario | Dos clientes separados, Email OTP y Google para cliente, usuario/contraseña y MFA selectivo para administración, y una Organization interna fueron configurados. No se registraron valores, URLs, IDs, secretos ni datos personales. | REQ-002-001 / REQ-002-004 | parcial: falta implementar y desplegar HTTPS `/login` y ejecutar la aceptación de invitación. |
| 2026-07-27 | Revisión documental: `node scripts/docs-check.mjs` con runtime integrado | Salida vacía y código 0; comprobación de referencias documentales superada. La comprobación de YAML específica no se ejecutó porque el runtime integrado no expone `js-yaml`; la sintaxis se mantendrá bajo revisión de los comandos del repositorio durante implementación. | NFR-002-001 | pass: revisión documental; no sustituye las pruebas de implementación. |
| 2026-07-28 | `pnpm --filter @pigar/config build`; `pnpm --filter @pigar/api build`; `node --test scripts/config.test.mjs` | Builds exitosos; Prisma Client generado desde el esquema de perfiles/auditoría. Las 2 pruebas de configuración pasaron y `.env.example` no contiene secretos. | REQ-002-002 / NFR-002-001 | pass parcial: base local y configuración OIDC; faltan guard, API y flujos. |
| 2026-07-28 | `pnpm --filter @pigar/config build`; `pnpm --filter @pigar/api build` | Builds exitosos tras incorporar el guard OIDC y `GET /v1/me`; el guard usa JWKS remoto y verifica RS256, issuer, audience y expiración antes de resolver un perfil local activo. | REQ-002-003 | pass parcial: compilación; faltan pruebas HTTP con JWT/JWKS sintéticos. |
| 2026-07-28 | `pnpm --filter @pigar/config build`; `pnpm --filter @pigar/api build`; `node --test scripts/config.test.mjs` | Builds exitosos y 2 pruebas de configuración pasadas tras sumar el aprovisionamiento idempotente de clientes y `PATCH /v1/me`. Un cliente administrativo no puede autoaprovisionarse: en producción se exige su client ID de Auth0. | REQ-002-002 / REQ-002-005 | pass parcial: compilación y configuración; faltan pruebas de integración con PostgreSQL y JWT/JWKS sintéticos. |
| 2026-07-28 | `pnpm --filter @pigar/admin-web build` | Build exitoso. `/login` es una ruta dinámica y el proxy Auth0 compila. El SDK informó que faltan credenciales de Auth0 en el entorno local, esperable porque no se versionan; no se ejecutó login real. | REQ-002-001 / AC-002-002 | pass parcial: implementación local; pendiente configuración HTTPS y prueba de invitación en staging. |
| 2026-07-28 | `pnpm --filter @pigar/api build` | Build exitoso tras sumar listado, cambio de rol y desactivación de perfiles internos. Las rutas exigen `ADMIN`, impiden auto-desactivación y rechazan retirar o desactivar el último ADMIN activo. | REQ-002-004 / REQ-002-006 | pass parcial: compilación; faltan pruebas HTTP, integración y concurrencia. |
| 2026-07-28 | `pnpm test:security --grep admin-profile-access` | 8 pruebas de la suite de seguridad pasaron; las 3 nuevas verifican que DISPATCHER no administra perfiles, que no se degrada el último ADMIN y que un ADMIN no se auto-desactiva. Fixtures sólo usan IDs sintéticos. | AC-002-005 | pass |
| 2026-07-28 | `pnpm test:security --grep auth-token-validation` | 7 pruebas de seguridad pasaron; la prueba de token confirma 401 para Authorization ausente, Basic o Bearer malformado y que no se consulta el perfil local. No contiene tokens reales. | AC-002-003 | pass parcial: faltan issuer/audience, expiración y JWKS sintéticos. |
| 2026-07-28 | `pnpm --filter @pigar/config build`; `pnpm --filter @pigar/api build`; `pnpm --filter @pigar/admin-web build`; `pnpm test:security --grep auth-invitation` | Builds exitosos. El adaptador usa credenciales de Management API exclusivamente desde variables de entorno, conserva el token de servicio sólo en memoria y las invitaciones se deduplican con `ClaimedJob`; `/login` permanece como callback dinámico. La prueba local confirma que una clave ya procesada no vuelve a llamar al adaptador. Los avisos del build de Next indican credenciales locales de Auth0 ausentes y no incluyen valores. | REQ-002-001 / REQ-002-004 / AC-002-002 | pass parcial: falta aceptación manual de invitación desde HTTPS de staging. |
| 2026-07-28 | `node --test scripts/config.test.mjs`; `node scripts/docs-check.mjs`; `pnpm lint`; `pnpm format:check`; `git diff --check` | 2 pruebas de configuración, comprobación documental, lint, formato y espacios en blanco finalizaron correctamente. Se verifica que el ejemplo declara el nombre de la variable secreta de Management API vacío, sin valores sensibles. | NFR-002-001 / NFR-002-004 | pass parcial: no sustituye integración con Auth0 ni la aceptación HTTPS de staging. |
| 2026-07-28 | `pnpm --filter @pigar/api build`; `pnpm test:security --grep auth-token-validation`; `pnpm test:security --grep inactive-and-worker-access` | Build exitoso. Las pruebas usan claves RSA y tokens sintéticos, sin proveedores ni secretos: se rechazaron firma/claves, issuer, audiencia y expiración inválidos antes de consultar perfiles; también se denegaron perfil inactivo y rol técnico fuera de PIGAR. | AC-002-003 / AC-002-006 | pass: cobertura local de validación de token y revocación; quedan concurrencia, logs, E2E de cliente e invitación HTTPS de staging. |
| 2026-07-28 | `pnpm --filter @pigar/api build`; `pnpm test:security --grep auth-log-sanitization` | Build exitoso y 7 pruebas de seguridad pasaron. La auditoría de identidad recibe el `X-Request-ID` validado; la prueba verifica que el evento no serializa autorización, token, email, teléfono ni otro dato sensible. Fixtures usan sólo IDs sintéticos. | AC-002-007 / NFR-002-004 | pass: sanitización y correlación locales; quedan concurrencia, E2E de cliente e invitación HTTPS de staging. |
| 2026-07-28 | `pnpm test:integration --grep profile-idempotency` | 6 pruebas de integración pasaron. Un proyecto Docker temporal aplicó las migraciones y ejecutó 16 altas concurrentes con un subject sintético: PostgreSQL conservó una sola fila `CLIENT` activa. Al finalizar se eliminaron contenedores y volúmenes del proyecto temporal. Durante la primera ejecución se detectó y corrigió que la comparación de un `adminClientId` ausente no debía bloquear a clientes. | AC-002-004 / REQ-002-002 | pass: idempotencia concurrente validada; quedan E2E de cliente e invitación HTTPS de staging. |
| 2026-07-28 | `pnpm --filter @pigar/customer-web build`; `pnpm test:e2e --grep auth-client`; `pnpm lint`; `pnpm format:check`; `git diff --check` | Build del portal cliente y 2 pruebas E2E locales pasaron. El portal inicia `/auth/login` mediante Auth0 SDK, con variables separadas de backoffice para cliente, secreto de sesión y callback; las variables se declaran vacías y no contienen valores. La prueba sólo inspecciona configuración local y no usa tokens, emails ni proveedores reales. | AC-002-001 / REQ-002-001 | pass parcial: OTP email y Google deben validarse desde Universal Login no productivo. |
| 2026-07-28 | `pnpm test:unit --grep staging-auth-configuration`; `pnpm lint`; `pnpm format:check`; `git diff --check`; `docker compose --env-file infra/hostinger/staging.env.example -f infra/hostinger/docker-compose.traefik.yml config --no-interpolate` | 6 pruebas unitarias pasaron. La plantilla de staging declara nombres separados para API, cliente y backoffice; Compose los inyecta exclusivamente en su superficie correspondiente. La validación de Compose terminó con código 0 y no creó servicios ni contactó el VPS. No se registraron secretos ni valores operativos. | REQ-002-001 / NFR-002-001 | pass parcial: faltan valores no productivos fuera de Git, URLs exactas en Auth0 y despliegue autorizado. |
| 2026-07-28 | `pnpm --filter @pigar/admin-web build`; `pnpm test:unit --grep staging-auth-configuration`; `pnpm lint`; `pnpm format:check`; `git diff --check` | Build del backoffice y 6 pruebas unitarias pasaron. Auth0 monta sus rutas administrativas bajo `/admin`, con `APP_BASE_URL` en el origen y una entrada pública exacta `/login` para invitaciones, entregada internamente a `/admin/login`. Se documentaron callbacks/logout/orígenes de staging sin valores operativos. | REQ-002-001 / AC-002-002 | pass parcial: el E2E técnico de Nginx no inició porque Docker Desktop no expuso su daemon; se reintentará al restablecerlo. |
| 2026-07-28 | `pnpm test:e2e --grep network-surface` | 2 pruebas E2E pasaron. Compose construyó y arrancó el entorno aislado; Nginx fue la única superficie publicada, cliente/backoffice/API respondieron los healthchecks técnicos y PostgreSQL/API sobrevivieron al reinicio. Se corrigió la configuración local de Compose con valores Auth0 sintéticos para que la API pueda validar su configuración de producción sin contactar proveedores. El proyecto y sus volúmenes temporales fueron eliminados al finalizar. | NFR-002-001 / AC-002-002 | pass parcial: no prueba OTP, Google ni invitación; esas verificaciones requieren las credenciales no productivas en staging HTTPS. |
| 2026-07-28 | `pnpm --filter @pigar/customer-web build`; `pnpm --filter @pigar/admin-web build` con el entorno de terminal que carga NVM | Ambos builds finalizaron con código 0 después de añadir la audiencia OIDC separada para cliente y backoffice. Next avisó que faltan credenciales de Auth0 durante el build local, esperable porque no se versionan ni se inyectaron. | REQ-002-001 / AC-002-003 | pass parcial: la audiencia y las rutas fueron preparadas; queda la prueba HTTPS autenticada en staging. |
| 2026-07-28 | `pnpm format:check`; `pnpm lint`; `pnpm typecheck`; `pnpm test:unit`; `node scripts/docs-check.mjs`; `git diff --check` con el entorno de terminal que carga NVM | Formato, lint, typecheck de 10 paquetes, 11 pruebas unitarias, comprobación documental y espacios en blanco finalizaron con código 0. Los avisos de PSReadLine son del perfil interactivo de PowerShell y no afectaron los comandos. | NFR-002-001 / NFR-002-004 | pass: puerta local previa a publicación. |

Cada resultado posterior registrará fecha, método o comando, salida resumida,
criterios cubiertos y la confirmación de que no expone credenciales ni datos
personales.

| 2026-07-29 | `pnpm --filter @pigar/config build`; `pnpm --filter @pigar/api build`; `pnpm test:security --grep auth-provisioning`; `pnpm test:e2e --grep auth-client`; `pnpm test:unit --grep staging-auth-configuration` | Compilación y 15 pruebas focalizadas superadas. El portal sólo inicia conexiones Google/OTP explícitas; el adaptador aprovisiona cuentas internas mediante Management API y la deduplicación no repite la operación procesada. No se usaron credenciales, tokens, emails reales ni proveedores externos. | AC-002-001 / AC-002-002 / REQ-002-004 | parcial: falta validar el acceso interno con MFA y los flujos reales en staging HTTPS. |
| 2026-07-29 | `pnpm format:write`; `pnpm lint`; `pnpm typecheck`; `pnpm test:unit`; `pnpm test:security`; `pnpm test:integration`; `pnpm test:e2e`; `node scripts/docs-check.mjs`; `git diff --check` | Formato, lint, tipos, 11 unitarias, 21 de seguridad, 11 de integración y 2 E2E superados. Integración/E2E usaron proyectos Docker temporales y se limpiaron al finalizar; Nginx siguió siendo la única superficie publicada. Sin credenciales, datos personales ni conexiones a Auth0. | NFR-002-001 / NFR-002-004 | pass local; la validación manual no productiva de Google, OTP y MFA sigue pendiente. |
| 2026-07-29 | Despliegue autorizado en Docker Manager de Hostinger y `Invoke-WebRequest` a `https://pigar.ferchamorro.cloud/api/health/ready` | Se restauró el compose versionado de staging, se configuró la conexión interna de Auth0 no productiva y Docker Manager informó el proyecto saludable. El endpoint externo respondió HTTP 200 con `service=api` y `status=ok`. No se registraron secretos, valores de variables, IDs ni datos personales. | REQ-002-001 / NFR-002-001 | pass parcial: API de staging disponible; quedan validaciones HTTPS autenticadas de Google, OTP y MFA. |
| 2026-07-29 | Despliegue autorizado en Docker Manager de Hostinger y `Invoke-WebRequest` a `/auth/login/email` con redirecciones deshabilitadas | Se configuró la conexión Email Passwordless existente de Auth0 para el portal de cliente y staging volvió a estado saludable. El inicio del flujo respondió HTTP 307, por lo que ya no devuelve `AUTH_UNAVAILABLE`; no se autenticó ningún usuario ni se envió información personal. La conexión Social de Google no existe aún en el tenant y no se configuró un sustituto. | AC-002-001 / REQ-002-001 | pass parcial: Email OTP disponible para prueba manual; Google y la validación HTTPS autenticada/MFA siguen pendientes. |
| 2026-07-29 | `node --test scripts/identity-admin.test.mjs`; `pnpm --filter @pigar/admin-web typecheck`; `git diff --check` con el runtime local | Las 6 pruebas focalizadas y el typecheck del backoffice finalizaron con código 0. La regresión verifica que `/admin/auth/callback` se entrega a Auth0 y conserva los controles administrativos existentes. La compilación de producción alcanzó `Compiled successfully` antes de que el límite local de 60 segundos interrumpiera su fase posterior; no se la registra como pass completo. No se usaron credenciales ni datos personales. | AC-002-002 / TEST-002-008 | pass local parcial: pendiente imagen de staging y validación HTTPS con MFA. |
| 2026-07-29 | Validación manual no productiva en staging por el usuario | El portal cliente completó el acceso mediante OTP email. La cuenta interna administrativa completó usuario/contraseña y MFA tras autorizar la aplicación para la API y retirar el requisito residual de Organizations. No se registraron usuarios, correos, códigos, tokens ni secretos. | AC-002-001B / AC-002-002A | pass parcial: Google queda pendiente por decisión explícita del usuario; no debe cerrarse AC-002-001A ni la feature hasta configurar y validar esa conexión. |
| 2026-08-01 | Decisión explícita del usuario | Google queda fuera del alcance del MVP de feat-002 y se reconsiderará antes de producción con decisión y validación no productiva separadas. La aceptación de OTP email y MFA se conserva; esta decisión no prueba ni autoriza cambios de código, proveedor o despliegue. | REQ-002-001 / AC-002-001 | alcance enmendado: Google deja de bloquear el cierre del MVP. |
| 2026-08-01 | Runtime Node integrado: `node scripts/run-test-suite.mjs unit --grep staging-auth-configuration`; `node scripts/run-test-suite.mjs e2e --grep auth-client`; `tsc --noEmit -p apps/customer-web/tsconfig.json`; `git diff --check`; búsqueda focalizada de la variable/conexión Google | Las 6 pruebas unitarias de la suite y las 2 E2E focalizadas pasaron; el typecheck del portal cliente y la comprobación de espacios finalizaron correctamente. La búsqueda no encontró la variable ni la conexión Google en código o configuración de producto; sólo quedaron aserciones negativas en las pruebas. No se usaron secretos, datos personales ni servicios externos. | REQ-002-001 / AC-002-001 / TASK-002-002 / TEST-002-001 | pass local: Google retirado de la superficie cliente y del staging versionado; falta validación sobre despliegue autorizado. |
| 2026-08-01 | `pnpm format:check`; `pnpm lint`; `pnpm typecheck`; `pnpm test:unit`; `node scripts/run-test-suite.mjs integration`; reintento `pnpm test:integration` con acceso ampliado | Formato, lint, typecheck de 10 paquetes y 11 unitarias superaron. La integración no es aprobada: los casos API/perfil no alcanzaron el servicio temporal porque Docker denegó el acceso al daemon; el reintento ampliado excedió 60 segundos sin salida concluyente. La misma suite registró un fallo `media-invalid/media-cross-access` ajeno a feat-002. No se registraron secretos, datos personales ni resultados de staging. | TASK-002-003 / TASK-002-006 / TEST-002-004 | bloqueado: no usar como evidencia de aceptación hasta restaurar la integración y diagnosticar el fallo ajeno. |
| 2026-08-01 | Con acceso al daemon Docker Desktop: `pnpm test:integration`; `pnpm test:security`; `pnpm test:e2e`; `node scripts/docs-check.mjs`; `git diff --check` | La integración pasó 11/11, incluida idempotencia concurrente de perfiles; seguridad pasó 22/22, incluyendo aprovisionamiento, tokens, revocación, auditoría y permisos; E2E pasó 2/2, incluida la superficie cliente OTP-only. Las pruebas temporales de Docker se limpiaron al finalizar. Los avisos de PSReadLine son del perfil interactivo y no afectaron los comandos. No se usaron secretos, datos personales ni staging. | TASK-002-003 / TASK-002-004 / TASK-002-005 / TASK-002-006 / TASK-002-007 / TEST-002-001 a TEST-002-008 | pass local: sustituye el bloqueo de integración anterior; falta validar la imagen en staging autorizadamente. |
| 2026-08-01 | `node --test scripts/identity-admin.test.mjs`; `pnpm --filter @pigar/admin-web typecheck`; `pnpm lint`; `git diff --check` | Las 6 pruebas de administración pasaron, incluida la regresión del enlace de inicio y callback bajo `/admin`; el typecheck del backoffice, lint y espacios en blanco finalizaron correctamente. Se eliminó la dependencia del proxy heredado `/login`, que habría dirigido el acceso administrativo al portal cliente. No se usaron secretos, datos personales, servicios externos ni staging. | AC-002-002 / TEST-002-008 | pass local: bloqueo de revisión independiente corregido; requiere nueva revisión independiente antes de publicar. |
| 2026-08-01 | Revisión independiente de Quality Reviewer | Revisión de requisitos, trazabilidad, cambios y evidencia aprobó el pase a `publication_review`. Confirmó que TEST-002-008 cubre el enlace `/admin/login`, `basePath: "/admin"` y la ausencia del proxy heredado; la validación de imagen en staging queda pendiente después de publicar, sin autorización de despliegue. | AC-002-002 / TEST-002-008 | pass de revisión independiente; habilita publicación autorizada. |
| 2026-08-01 | Despliegue autorizado del proyecto existente `pigar-staging` con `PIGAR_IMAGE_TAG=1589fd915717193705b1cea262af52f7aa5b951a`; smoke HTTP externo | Docker Manager informó el despliegue en curso y las rutas `/`, `/admin`, `/health/live` y `/api/health/ready` respondieron HTTP 200. Los puertos 3000 y 5432 no aceptaron conexiones públicas. No se registraron variables de entorno, secretos, identificadores operativos ni datos personales. | NFR-002-001 / TASK-002-001 / TASK-002-002 / TASK-002-006 | pass de staging: imagen publicada disponible y superficie de red esperada. No sustituye la aceptación funcional manual de identidad. |
| 2026-08-02 | Validación manual no productiva en staging por el usuario, sobre la imagen desplegada | El usuario confirmó que el acceso cliente por OTP email y el acceso administrativo con usuario/contraseña y MFA completaron correctamente. Confirmó también que no se presentó Google en ningún momento. No se registraron usuarios, correos, códigos, tokens, secretos ni datos personales. | AC-002-001 / AC-002-002 / TEST-002-001 / TEST-002-002 | pass: aceptación funcional final de la imagen de staging; Google permanece fuera del MVP. |
| 2026-08-02 | `rg -n --fixed-strings '[ ]' specs/features/feat-002/tasks.md`; `git diff --check` | La búsqueda informó `tasks_open=0` y la comprobación de espacios finalizó sin hallazgos. Los avisos de fin de línea de Git no alteran el contenido. | Definition of done / TASK-002-001 a TASK-002-007 | pass: artefactos de cierre sin tareas abiertas ni errores de espacios. |
